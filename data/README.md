# Local data caches

Price / artwork / DLC caches are written here at runtime (`npm run refresh:prices`, dashboard sync).

This folder is gitignored except for this placeholder. Safe to delete contents anytime — they regenerate.
