# Local Steam samples

After you run the fetch/parse scripts, this directory holds:

- `account-data/` — raw Account Data HTML from Steam
- `parsed/` — JSON fixtures used by the dashboard

**Gitignored** — may contain purchase history and Steam IDs. Never commit or publish these files.
